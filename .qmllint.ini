# `sddm`, `config`, `userModel` and `sessionModel` are context properties the
# SDDM greeter injects at runtime; qmllint cannot see them, so every theme
# trips this category. Everything else stays on.
[General]
AdditionalQmlImportPaths=
DisableDefaultImports=false

[Warnings]
UnqualifiedAccess=disable
